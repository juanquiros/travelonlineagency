{% extends 'administrador/base.html.twig' %}

{% block title %}Panel - Mensajes{% endblock %}

{% block body %}
    <div class="" style="background-color: white; box-shadow: 2px 2px 2px rgba(0,0,0,0.3)">
        <div class="row" >
            <div class="col-md-3"></div>
            <div class="col-md-6 pt-5" style=" min-width: 410px !important;">
                <h1>Reservas para {{ booking.nombre }}</h1>
            </div>
            <div class="col-md-3"></div>
        </div>
        <hr>
        <div class="row">
            <div class="col-md-2"></div>
            <div class="col-md-8 p-5" style="text-align: left; min-width: 410px !important;">
                <h3>{{ reservas['pagadas'] | length }} Solicitudes confirmadas</h3>
                <div class="row">
                    <div class="col-md-4">
                        {% if personas and personas['pagadas'] %}
                            <table>
                                <thead><tr><th>Fecha</th><th>Personas</th></tr></thead>
                                <tbody>
                                {% for fecha in personas['pagadas'] %}
                                    <tr><td>{{ fecha['fecha'] | date('d-m-Y H:i')}}</td><td style="text-align: center">{{ fecha['solicitudes'] }}</td></tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        {% endif %}
                    </div>
                </div>
                <nav class="navbar navbar-expand-lg navbar-light bg-light">
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <div class="collapse navbar-collapse" id="navbarSupportedContent">
                        <ul class="navbar-nav mr-auto">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_pdf_generator',{id:booking.id,estadoId:2,ff:fechaFiltro}) }}"><box-icon type='solid' name='file-pdf'></box-icon>Generar pdf{% if fechaFiltro  %} para fecha "{{ fechaFiltro | date("d/m/Y H:i","America/Argentina/Buenos_Aires") }}" {% else %} de todas las fechas{% endif %}</a>
                            </li>
                        </ul>
                        <form class="form-inline my-2 my-lg-0">
                            <label for="filtroFecha" class="mr-5">Filtrar</label>

                            <select id="filtroFecha" onchange="filtrarFechaSolicitudesBooking({{ booking.id }},'filtroFecha')" class="form-control">
                                <option selected value="0">Todos</option>
                                {%set arrayDate = booking.getFechasdelservicioArray %}
                                {% for date in arrayDate %}

                                    <option
                                        {% if fechaFiltro and (fechaFiltro == (date.fecha | date("d-m-Y H:i","America/Argentina/Buenos_Aires"))) %}
                                            selected
                                        {% endif %}
                                    >{{ date.fecha | date("d/m/Y H:i","America/Argentina/Buenos_Aires") }}</option>

                                {% endfor %}
                            </select>
                        </form>
                    </div>
                </nav>
                <hr>
                <table class="table" style="box-shadow: 1px 1px 4px rgba(0,0,0,0.4 )">
                    <thead>
                    <tr id="theadTr">
                        <th scope="col" class="th-bookings-1">*</th>
                        <th scope="col" class="th-bookings-2">Solicitado por</th>
                        <th scope="col" class="th-bookings-3">Personas</th>
                        <th scope="col" class="th-bookings-4">Fecha</th>
                        <th scope="col" class="th-bookings-5">Pedido el</th>
                    </tr>
                    </thead>
                    <tbody class="table-group-divider">
                    <div class="accordion" id="accordionExample">

                    {% for reserva in reservas['pagadas'] %}
                        <tr>

                            <td colspan="6">
                                <div class="accordion">
                                    <div class="accordion-item">
                                        <h2 class="accordion-header" id="heading{{ reserva.id }}">
                                            <button class="accordion-button collapsed"  type="button" data-bs-toggle="collapse" data-bs-target="#collapse{{ reserva.id }}" aria-expanded="false" aria-controls="collapse{{ reserva.id }}">
                                                <table style="margin: 0 auto; padding: 0; width: 100%">
                                                    <td class="td-bookings-1">{{ reserva.id }}</td>
                                                    <td class="td-bookings-2">{{ reserva.name }} {{ reserva.surname }}</td>
                                                    <td class="td-bookings-3" style="display: inline-flex;justify-items: baseline"><box-icon name='group'></box-icon> {{ reserva.getInChargeOfArray | length + 1 }}</td>
                                                    <td class="td-bookings-4">{% if reserva.fechaSeleccionada %}{{ reserva.fechaSeleccionada | date("d/m/Y H:i","America/Argentina/Buenos_Aires") }}{% endif %}</td>
                                                    <td class="td-bookings-5">{{ reserva.getUpdatedAt | date("d/m/Y H:i","America/Argentina/Buenos_Aires") }}</td>
                                                </table>
                                            </button>
                                        </h2>
                                        <div id="collapse{{ reserva.id }}" class="accordion-collapse collapse" aria-labelledby="heading{{ reserva.id }}" data-bs-parent="#accordionExample">
                                            <div class="accordion-body" style="background-color: whitesmoke">



                                                <p>Estado: {{ reserva.estado.getnombreporlenguaje(idiomaPlataforma.codigo).traduccion }}</p>
                                                <p><strong>Datos del solicitante</strong></p>
                                                <table class="table table-borderless">
                                                    <thead>
                                                        <tr>
                                                            <th>Nombre</th>
                                                            <th>Apellido</th>
                                                            <th>Email</th>
                                                            <th>Telefono</th>
                                                            {% for dato in reserva.getFormRequiredArray %}
                                                                <th>{{ dato.dato }}</th>
                                                            {% endfor %}
                                                        </tr>
                                                    </thead>
                                                    <tbody>
                                                        <tr>
                                                            <td>{{ reserva.name }}</td>
                                                            <td>{{ reserva.surname }}</td>
                                                            <td>{{ reserva.email }}</td>
                                                            <td><a href="https://wa.me/{{ reserva.phone }}">{{ reserva.phone }}</a></td>
                                                            {% for dato in reserva.getFormRequiredArray %}
                                                                <td>{{ dato.value }}</td>
                                                            {% endfor %}
                                                        </tr>
                                                    </tbody>
                                                </table>

                                                {% if reserva.getInChargeOfArray | length > 0 %}
                                                    <div class="row">
                                                        <div class="col-md-6">
                                                            <p><strong>Solicitudes adicionales</strong></p>
                                                        </div>
                                                        <div class="col-md-6"></div>
                                                    </div>
                                                    <table class="table table-sm">
                                                        <thead>
                                                        <tr>
                                                            <th  scope="col" >Nombre</th>
                                                            <th  scope="col" >Apellido</th>
                                                            {% for dato in reserva.getInChargeOfArray[0].form_required %}
                                                                <th  scope="col" >{{ dato.dato }}</th>
                                                            {% endfor %}
                                                        </tr>
                                                        </thead>
                                                        <tbody>

                                                        {% for adiccional in reserva.getInChargeOfArray %}
                                                            <tr>
                                                                <td>{{ adiccional.name }}</td>
                                                                <td>{{ adiccional.surname }}</td>
                                                                {% for dato in adiccional.form_required %}
                                                                    <td>{{ dato.value }}</td>
                                                                {% endfor %}
                                                            </tr>
                                                        {% endfor %}
                                                        </tbody>
                                                    </table>
                                                {% endif %}
                                                {% if reserva.pagosPayPal or reserva.pagosMercadoPago %}
                                                    <table class="table table-sm">
                                                        <thead>
                                                        <tr>
                                                            <th  scope="col" >*</th>
                                                            <th  scope="col" >Referencia</th>
                                                            <th  scope="col" >Estado</th>
                                                            <th  scope="col" >Valor</th>

                                                        </tr>
                                                        </thead>
                                                        <tbody>
                                                        {% for pago in reserva.pagosMercadoPago %}
                                                            <tr>
                                                                <td>{{ pago.updatedAt  | date("d/m/Y H:i","America/Argentina/Buenos_Aires")}}</td>
                                                                <td>{{ pago.paymentId  }}</td>
                                                                <td>{{ pago.status }}</td>
                                                                <td>

                                                                    ARS ${{ pago.transactionAmount - pago.transactionAmountRefunded }}</td>
                                                            </tr>
                                                            <tr style="font-size: 0.8rem; color:gray !important;">
                                                                <td>Pago:${{ pago.transactionAmount }}</td>
                                                                <td>Refunded:${{ pago.transactionAmountRefunded }}</td>
                                                                <td>Metodo:{{ pago.paymentTypeId }}</td>
                                                                <td>
                                                                    Recibido:${{ pago.netReceivedAmount }}
                                                                    {% if pago.getFeeDetailsArray | length > 0 %}{% for detalle in pago.getFeeDetailsArray %}{{ detalle.type }}(${{ detalle.amount }}),{% endfor %}{% endif %}
                                                                </td>
                                                            </tr>
                                                        {% endfor %}
                                                        {% for pago in reserva.pagosPayPal %}
                                                            <tr>
                                                                <td>{{ pago.updatedAt  | date("d/m/Y H:i","America/Argentina/Buenos_Aires")}}</td>
                                                                <td>{{ pago.ordersId  }}</td>
                                                                <td>{{ pago.estado }}</td>
                                                                <td>USD {{ pago.total }}</td>
                                                            </tr>
                                                        {% endfor %}
                                                        </tbody>
                                                    </table>
                                                {% endif %}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                    </div>

                    </tbody>
                </table>

            </div>
            <div class="col-md-2"></div>
        </div>
        <hr>
        <div class="row">
            <div class="col-md-2"></div>
            <div class="col-md-8 p-5" style="text-align: left; min-width: 410px !important;">
                <h3>{{ reservas['pendientes'] | length }} Solicitudes pendientes de pago</h3>
                <div class="row">
                    <div class="col-md-4">
                        {% if personas and personas['pendientes'] %}
                            <table>
                                <thead><tr><th>Fecha</th><th>Personas</th></tr></thead>
                                <tbody>
                                {% for fecha in personas['pendientes'] %}
                                    <tr><td>{{ fecha['fecha'] | date('d-m-Y H:i')}}</td><td style="text-align: center">{{ fecha['solicitudes'] }}</td></tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        {% endif %}
                    </div>
                </div>
                <nav class="navbar navbar-expand-lg navbar-light bg-light">
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-target="#navbarSupportedContent2" aria-controls="navbarSupportedContent2" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <div class="collapse navbar-collapse" id="navbarSupportedContent2">
                        <ul class="navbar-nav mr-auto">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_pdf_generator',{id:booking.id,estadoId:1,ff:fechaFiltro}) }}"><box-icon type='solid' name='file-pdf'></box-icon>Generar pdf{% if fechaFiltro  %} para fecha "{{ fechaFiltro | date("d/m/Y H:i","America/Argentina/Buenos_Aires") }}" {% else %} de todas las fechas{% endif %}</a>
                            </li>
                        </ul>
                        <form class="form-inline my-2 my-lg-0">
                            <label for="filtroFecha2" class="mr-5">Filtrar</label>
                            <select id="filtroFecha2" onchange="filtrarFechaSolicitudesBooking({{ booking.id }},'filtroFecha2')" class="form-control">
                                <option selected>Todos</option>
                                {%set arrayDate = booking.getFechasdelservicioArray %}
                                {% for date in arrayDate %}
                                    <option
                                            {% if fechaFiltro and (fechaFiltro == (date.fecha | date("d-m-Y H:i","America/Argentina/Buenos_Aires"))) %}
                                                selected
                                            {% endif %}
                                    >{{ date.fecha | date("d/m/Y H:i","America/Argentina/Buenos_Aires") }}</option>
                                {% endfor %}
                            </select>
                        </form>
                    </div>
                </nav>
                <hr>
                <table class="table" style="box-shadow: 1px 1px 4px rgba(0,0,0,0.4 )">
                    <thead>
                    <tr>
                        <th scope="col" class="th-bookings-1">*</th>
                        <th scope="col" class="th-bookings-2">Solicitado por</th>
                        <th scope="col" class="th-bookings-3">Personas</th>
                        <th scope="col" class="th-bookings-4">Fecha</th>
                        <th scope="col" class="th-bookings-5">Pedido el</th>
                    </tr>
                    </thead>
                    <tbody class="table-group-divider">
                    <div class="accordion" id="accordion-Example">

                        {% for reserva in reservas['pendientes'] %}
                        <tr>

                            <td colspan="6">
                                <div class="accordion">
                                    <div class="accordion-item">
                                        <h2 class="accordion-header" id="heading-{{ reserva.id }}">
                                            <button class="accordion-button collapsed"  type="button" data-bs-toggle="collapse" data-bs-target="#collapse-{{ reserva.id }}" aria-expanded="false" aria-controls="collapse-{{ reserva.id }}">
                                                <table style="margin: 0 auto; padding: 0; width: 100%">
                                                    <td class="td-bookings-1">{{ reserva.id }}</td>
                                                    <td class="td-bookings-2">{{ reserva.name }} {{ reserva.surname }}</td>
                                                    <td class="td-bookings-3" style="display: inline-flex;justify-items: baseline"><box-icon name='group'></box-icon> {{ reserva.getInChargeOfArray | length + 1 }}</td>
                                                    <td class="td-bookings-4">{% if reserva.fechaSeleccionada %}{{ reserva.fechaSeleccionada | date("d/m/Y H:i","America/Argentina/Buenos_Aires") }}{% endif %}</td>
                                                    <td class="td-bookings-5">{{ reserva.getUpdatedAt | date("d/m/Y H:i","America/Argentina/Buenos_Aires") }}</td>
                                                </table>
                                            </button>
                                        </h2>
                                        <div id="collapse-{{ reserva.id }}" class="accordion-collapse collapse" aria-labelledby="heading{{ reserva.id }}" data-bs-parent="#accordion-Example">
                                            <div class="accordion-body">
                                                <p>Estado: {{ reserva.estado.getnombreporlenguaje(idiomaPlataforma.codigo).traduccion }}</p>
                                                <p><strong>Datos del solicitante</strong></p>
                                                <table class="table table-borderless">
                                                    <thead>
                                                    <tr>
                                                        <th>Nombre</th>
                                                        <th>Apellido</th>
                                                        <th>Email</th>
                                                        <th>Telefono</th>
                                                        {% for dato in reserva.getFormRequiredArray %}
                                                            <th>{{ dato.dato }}</th>
                                                        {% endfor %}
                                                    </tr>
                                                    </thead>
                                                    <tbody>
                                                    <td>{{ reserva.name }}</td>
                                                    <td>{{ reserva.surname }}</td>
                                                    <td>{{ reserva.email }}</td>
                                                    <td>{{ reserva.phone }}</td>
                                                    {% for dato in reserva.getFormRequiredArray %}
                                                        <td>{{ dato.value }}</td>
                                                    {% endfor %}
                                                    </tbody>
                                                </table>
                                                {% if reserva.getInChargeOfArray | length > 0 %}
                                                    <div class="row">
                                                        <div class="col-md-6">
                                                            <p><strong>Solicitudes adicionales</strong></p>
                                                        </div>
                                                        <div class="col-md-6"></div>
                                                    </div>
                                                    <table class="table table-sm">
                                                        <thead>
                                                        <tr>
                                                            <th  scope="col" >Nombre</th>
                                                            <th  scope="col" >Apellido</th>
                                                            {% for dato in reserva.getInChargeOfArray[0].form_required %}
                                                                <th  scope="col" >{{ dato.dato }}</th>
                                                            {% endfor %}
                                                        </tr>
                                                        </thead>
                                                        <tbody>

                                                        {% for adiccional in reserva.getInChargeOfArray %}
                                                            <tr>
                                                                <td>{{ adiccional.name }}</td>
                                                                <td>{{ adiccional.surname }}</td>
                                                                {% for dato in adiccional.form_required %}
                                                                    <td>{{ dato.value }}</td>
                                                                {% endfor %}
                                                            </tr>
                                                        {% endfor %}
                                                        </tbody>
                                                    </table>
                                                {% endif %}
                                                {% if reserva.pagosPayPal %}
                                                    <table class="table table-sm">
                                                        <thead>
                                                        <tr>
                                                            <th  scope="col" >*</th>
                                                            <th  scope="col" >Referencia</th>
                                                            <th  scope="col" >Estado</th>
                                                            <th  scope="col" >Valor</th>
                                                            <th  scope="col" >Acción</th>

                                                        </tr>
                                                        </thead>
                                                        <tbody>

                                                        {% for pago in reserva.pagosPayPal %}
                                                            <tr>
                                                                <td>{{ pago.updatedAt  | date("d/m/Y H:i","America/Argentina/Buenos_Aires")}}</td>
                                                                <td>{{ pago.ordersId  }}</td>
                                                                <td>{{ pago.estado }}</td>
                                                                <td>USD {{ pago.total }}</td>
                                                                <td><a href="#">Cancelar orden</a></td>
                                                            </tr>
                                                        {% endfor %}
                                                        </tbody>
                                                    </table>
                                                {% endif %}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    </div>
                    {% endfor %}
                    </tbody>
                    </tbody>
                </table>

            </div>
            <div class="col-md-2"></div>
        </div>



        <hr>
        <div class="row">
            <div class="col-md-2"></div>
            <div class="col-md-8 p-5" style="text-align: left; min-width: 410px !important;">
                <h3>{{ reservas['canceladas'] | length }} Solicitudes canceladas</h3>
                <div class="row">
                    <div class="col-md-4">
                        {% if personas and personas['canceladas'] %}
                            <table>
                                <thead><tr><th>Fecha</th><th>Personas</th></tr></thead>
                                <tbody>
                                {% for fecha in personas['canceladas'] %}
                                    <tr><td>{{ fecha['fecha'] | date('d-m-Y H:i')}}</td><td style="text-align: center">{{ fecha['solicitudes'] }}</td></tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        {% endif %}
                    </div>
                </div>
                <nav class="navbar navbar-expand-lg navbar-light bg-light">
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-target="#navbarSupportedContent3" aria-controls="navbarSupportedContent3" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <div class="collapse navbar-collapse" id="navbarSupportedContent3">
                        <ul class="navbar-nav mr-auto">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_pdf_generator',{id:booking.id,estadoId:3,ff:fechaFiltro}) }}"><box-icon type='solid' name='file-pdf'></box-icon>Generar pdf{% if fechaFiltro  %} para fecha "{{ fechaFiltro | date("d/m/Y H:i","America/Argentina/Buenos_Aires") }}" {% else %} de todas las fechas{% endif %}</a>
                            </li>
                        </ul>
                        <form class="form-inline my-2 my-lg-0">
                            <label for="filtroFecha3" class="mr-5">Filtrar</label>
                            <select id="filtroFecha3" onchange="filtrarFechaSolicitudesBooking({{ booking.id }},'filtroFecha3')" class="form-control">
                                <option>Todos</option>
                                {%set arrayDate = booking.getFechasdelservicioArray %}
                                {% for date in arrayDate %}
                                    <option
                                            {% if fechaFiltro and (fechaFiltro == (date.fecha | date("d-m-Y H:i","America/Argentina/Buenos_Aires"))) %}
                                                selected
                                            {% endif %}
                                    >{{ date.fecha | date("d/m/Y H:i","America/Argentina/Buenos_Aires") }}</option>
                                {% endfor %}
                            </select>
                        </form>
                    </div>
                </nav>
                <hr>
                <table class="table" style="box-shadow: 1px 1px 4px rgba(0,0,0,0.4 )">
                    <thead>
                    <tr>
                        <th scope="col" class="th-bookings-1">*</th>
                        <th scope="col" class="th-bookings-2">Solicitado por</th>
                        <th scope="col" class="th-bookings-3">Personas</th>
                        <th scope="col" class="th-bookings-4">Fecha</th>
                        <th scope="col" class="th-bookings-5">Pedido el</th>
                    </tr>
                    </thead>
                    <tbody class="table-group-divider">
                    <div class="accordion" id="accordion-Example">

                        {% for reserva in reservas['canceladas'] %}
                        <tr>

                            <td colspan="6">
                                <div class="accordion">
                                    <div class="accordion-item">
                                        <h2 class="accordion-header" id="heading-{{ reserva.id }}">
                                            <button class="accordion-button collapsed"  type="button" data-bs-toggle="collapse" data-bs-target="#cancelado-collapse-{{ reserva.id }}" aria-expanded="false" aria-controls="cancelado-collapse-{{ reserva.id }}">
                                                <table style="margin: 0 auto; padding: 0; width: 100%">
                                                    <td class="td-bookings-1">{{ reserva.id }}</td>
                                                    <td class="td-bookings-2">{{ reserva.name }} {{ reserva.surname }}</td>
                                                    <td class="td-bookings-3" style="display: inline-flex;justify-items: baseline"><box-icon name='group'></box-icon> {{ reserva.getInChargeOfArray | length + 1 }}</td>
                                                    <td class="td-bookings-4">{% if reserva.fechaSeleccionada %}{{ reserva.fechaSeleccionada | date("d/m/Y H:i","America/Argentina/Buenos_Aires") }}{% endif %}</td>
                                                    <td class="td-bookings-5">{{ reserva.getUpdatedAt | date("d/m/Y H:i","America/Argentina/Buenos_Aires") }}</td>
                                                </table>
                                            </button>
                                        </h2>
                                        <div id="cancelado-collapse-{{ reserva.id }}" class="accordion-collapse collapse" aria-labelledby="cancelado-heading{{ reserva.id }}" data-bs-parent="#accordion-Example2">
                                            <div class="accordion-body">
                                                <p>Estado: {{ reserva.estado.getnombreporlenguaje(idiomaPlataforma.codigo).traduccion }}</p>
                                                <p><strong>Datos del solicitante</strong></p>
                                                <table class="table table-borderless">
                                                    <thead>
                                                    <tr>
                                                        <th>Nombre</th>
                                                        <th>Apellido</th>
                                                        <th>Email</th>
                                                        <th>Telefono</th>
                                                        {% for dato in reserva.getFormRequiredArray %}
                                                            <th>{{ dato.dato }}</th>
                                                        {% endfor %}
                                                    </tr>
                                                    </thead>
                                                    <tbody>
                                                    <td>{{ reserva.name }}</td>
                                                    <td>{{ reserva.surname }}</td>
                                                    <td>{{ reserva.email }}</td>
                                                    <td>{{ reserva.phone }}</td>
                                                    {% for dato in reserva.getFormRequiredArray %}
                                                        <td>{{ dato.value }}</td>
                                                    {% endfor %}
                                                    </tbody>
                                                </table>
                                                {% if reserva.getInChargeOfArray | length > 0 %}
                                                    <div class="row">
                                                        <div class="col-md-6">
                                                            <p><strong>Solicitudes adicionales</strong></p>
                                                        </div>
                                                        <div class="col-md-6"></div>
                                                    </div>
                                                    <table class="table table-sm">
                                                        <thead>
                                                        <tr>
                                                            <th  scope="col" >Nombre</th>
                                                            <th  scope="col" >Apellido</th>
                                                            {% for dato in reserva.getInChargeOfArray[0].form_required %}
                                                                <th  scope="col" >{{ dato.dato }}</th>
                                                            {% endfor %}
                                                        </tr>
                                                        </thead>
                                                        <tbody>

                                                        {% for adiccional in reserva.getInChargeOfArray %}
                                                            <tr>
                                                                <td>{{ adiccional.name }}</td>
                                                                <td>{{ adiccional.surname }}</td>
                                                                {% for dato in adiccional.form_required %}
                                                                    <td>{{ dato.value }}</td>
                                                                {% endfor %}
                                                            </tr>
                                                        {% endfor %}
                                                        </tbody>
                                                    </table>
                                                {% endif %}
                                                {% if reserva.pagosPayPal %}
                                                    <table class="table table-sm">
                                                        <thead>
                                                        <tr>
                                                            <th  scope="col" >*</th>
                                                            <th  scope="col" >Referencia</th>
                                                            <th  scope="col" >Estado</th>
                                                            <th  scope="col" >Valor</th>
                                                            <th  scope="col" >Acción</th>

                                                        </tr>
                                                        </thead>
                                                        <tbody>

                                                        {% for pago in reserva.pagosPayPal %}
                                                            <tr>
                                                                <td>{{ pago.updatedAt  | date("d/m/Y H:i","America/Argentina/Buenos_Aires")}}</td>
                                                                <td>{{ pago.ordersId  }}</td>
                                                                <td>{{ pago.estado }}</td>
                                                                <td>USD {{ pago.total }}</td>
                                                                <td>-</td>
                                                            </tr>
                                                        {% endfor %}
                                                        </tbody>
                                                    </table>
                                                {% endif %}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    </div>
                    {% endfor %}
                    </tbody>
                </table>

            </div>
            <div class="col-md-2"></div>
        </div>
    </div>
    <script>
        var arreglo = [].slice.call(document.getElementById('theadTr').children);
        arreglo.forEach((tdVar)=> {
            id = tdVar.classList[0].split('-')[2]
            const uno = document.querySelector('.th-bookings-'+id);
            var dos = [].slice.call(document.getElementsByClassName('td-bookings-'+id));
            dos.forEach((dosvar)=>{
                const {width} = uno.getBoundingClientRect();
                dosvar.style.width = `${width-5}px`;
            })

        })
        $(function () {
            $('[data-toggle="tooltip"]').tooltip()
        })
    </script>
{% endblock %}
