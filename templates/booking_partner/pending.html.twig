{% extends 'base.html.twig' %}

{% block title %}Acceso a partners{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card border-0 shadow-sm">
                    <div class="card-body p-5 text-center">
                        <h1 class="h3 mb-3">Panel de partners</h1>
                        {% if reason == 'pending' %}
                            <p class="text-muted mb-4">Tu cuenta está registrada como partner y se encuentra pendiente de autorización del administrador. Te avisaremos cuando puedas comenzar a publicar servicios.</p>
                        {% elseif reason == 'role-missing' %}
                            <p class="text-muted mb-4">Detectamos que tu cuenta está aprobada pero aún no cuenta con los permisos necesarios. Ponete en contacto con el administrador para completar la activación.</p>
                        {% else %}
                            <p class="text-muted mb-4">Para acceder al panel de partners necesitás solicitar la habilitación desde tu perfil. Registrate o escribí a soporte para que el equipo de Travel Online Agency active tu cuenta.</p>
                        {% endif %}
                        <a class="btn btn-outline-primary" href="{{ path('app_inicio') }}">Volver al inicio</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
