{% set nav = navigation ?? {} %}
<div class="mb-4">
    <ul class="nav nav-pills flex-column flex-md-row gap-2">
        <li class="nav-item">
            <a class="nav-link {{ nav.services ? 'active' : '' }}" href="{{ path('app_booking_partner') }}">
                <i class="bi bi-briefcase me-1"></i> Mis servicios
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link {{ nav.balance ? 'active' : '' }}" href="{{ path('app_booking_partner_balance') }}">
                <i class="bi bi-wallet2 me-1"></i> Balance
            </a>
        </li>
    </ul>
</div>
