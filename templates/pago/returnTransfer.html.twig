{% extends 'base.html.twig' %}

{% block title %}Pago de traslado{% endblock %}

{% block body %}
<section class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-7">
            <div class="card shadow-sm">
                <div class="card-body text-center">
                    <h1 class="h4 mb-3">¡Gracias por tu pago!</h1>
                    <p class="text-muted">Registramos el pago de tu traslado. Podés seguir el estado del viaje y compartir el código de seguimiento con tu chofer.</p>
                    <div class="my-4">
                        <div class="fw-semibold">Código de seguimiento</div>
                        <div class="display-6">{{ solicitud.tokenSeguimiento }}</div>
                    </div>
                    <a class="btn btn-primary w-100 mb-2" href="{{ trackingUrl }}" target="_blank">Ver seguimiento</a>
                    <a class="btn btn-outline-secondary w-100" href="{{ path('app_transfer_summary', {token: solicitud.tokenSeguimiento}) }}">Volver al resumen</a>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}
